# Dépendances Python de l'image worker Prefect
# Pinned pour la reproductibilité — mettre à jour via `pip-compile`

# ── Orchestration ──────────────────────────────────────────────────────────────
prefect>=3.0.0,<4.0
prefect-kubernetes>=0.4.0

# ── Données & compute ──────────────────────────────────────────────────────────
requests>=2.32.0
pandas>=2.2.0
pyarrow>=16.0.0
duckdb>=1.0.0
polars>=1.0.0

# ── Iceberg + MinIO ────────────────────────────────────────────────────────────
pyiceberg[s3fs]>=0.7.0
s3fs>=2024.6.0
boto3>=1.34.0

# ── MLflow tracking ────────────────────────────────────────────────────────────
mlflow>=2.14.0

# ── Base de données ────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9         # TimescaleDB + PostgreSQL
sqlalchemy>=2.0.0              # ORM MLflow backend

# ── Géospatial ────────────────────────────────────────────────────────────────
pyproj>=3.6.0                  # Lambert 93 → WGS84 (Sandre)

# ── Sources optionnelles ───────────────────────────────────────────────────────
# Décommenter selon les familles activées dans le flow :
# cdsapi>=0.7.0                # CAMS Copernicus
# xarray>=2024.6.0             # Parsing NetCDF CAMS
# netCDF4>=1.7.0               # Fallback NetCDF reader
# openaq>=1.0.0                # OpenAQ SDK (aeolus extra)
# purpleair-api>=0.1.0         # PurpleAir SDK (aeolus extra)
